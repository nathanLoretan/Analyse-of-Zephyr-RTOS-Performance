TODO:

- mtu request baremetal
- adc correct gain *2
- click notification
- nbr packets zephyr
- Softdevice RAM
- Time between different packet
- Adv packet transmition
- Central BLE interface
- RTT
- BM, thread priority
- BLE See if 1MBit/s or 2MBit/s

git config --global -e
git commit -a -s
git commit --amend
git branch -a
git branch -aav
git remote add nathan https://github.com/nathanLoretan/zephyr
git remote -m origin upstream
git remote rename origin upstream
git fetch --all -p
git checkout -b nathan_fix_spim_config
git rebase upstream/master
git log
git show
git push -u nathan
git push --set-upstream nathan nathan_fix_spim_config


READ:
NRF52840 datashee       p289
BLE core specification  p2568 link layer specification
