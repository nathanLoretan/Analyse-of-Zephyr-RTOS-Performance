TODO:

2. Central BLE interface

3. RTT

4. Code Improvement
- BM, thread priority

5. BLE See if 1MBit/s or 2MBit/s

git config --global -e
git commit -a -s
git commit --amend
git branch -a
git branch -aav
git remote add nathan https://github.com/nathanLoretan/zephyr
git remote -m origin upstream
git remote rename origin upstream
git fetch --all -p
git checkout -b nathan_fix_spim_config
git rebase upstream/master
git log
git show
git push -u nathan
git push --set-upstream nathan nathan_fix_spim_config
